CREATE TABLE IF NOT EXISTS cortage (
                                     id bigserial PRIMARY KEY NOT NULL,
                                     id_user BIGINT NOT NULL,
                                     id_team BIGINT NOT NULL
);